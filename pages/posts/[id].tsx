import type { NextPage } from "next";
import { useRouter } from "next/router";

const PostPage: NextPage = () => {
  // const router = useRouter();
  // const { id } = router.query;

  return <main>any sub route</main>;
};

export default PostPage;
